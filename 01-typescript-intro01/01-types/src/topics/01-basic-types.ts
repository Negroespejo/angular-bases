const name = 'Strider';

let hpPoints: number | 'Full' = 95;
//hpPoints =  'hola mundo'

const isAlive: boolean = true;

console.log({
    name,
    hpPoints,
    isAlive
});

export{}; //exporta un obejto